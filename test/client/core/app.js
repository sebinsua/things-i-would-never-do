define([], function () {
    'use strict';

    describe('Sanity Test', function () {

        it('should pass this', function () {
            expect(1).to.equal(1);
        });

        it('and this', function () {
            expect(1).to.equal(1);
        });
    });
    
});